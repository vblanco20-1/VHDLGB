- alu ops test program. should write 8 to adress 0 and 1F at adress 1
- with registers A and B as a=0x10
$byte firstvar = 0x00
$byte secondvar = 0x00
ld a $fullvar
and a #0x07
ld b #0x01
add a b
ld hl @firstvar
ld (hl) a
inc l
or a #0x1F
- compare a with 12, which is bigger. Should carry
cmp a #0x12
jmp c :branch
ld a #0xFF 

:branch
ld (hl) a
xor a #0x0F

$byte halt = 0x76
$byte fullvar = 0xFF